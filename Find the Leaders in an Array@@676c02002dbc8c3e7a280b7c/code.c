#include <stdio.h>
int main()
{
    int n,i;
    scanf("%d",&n);
    int arr[n];
    for(int i=0;i<n;i++)
    {
        scanf("%d",&arr[i]);
    }
    max=arr[n-1];
    printf("%d",&arr[n-1]);
    for(int i=n-1;i>=0;i--)
    {
        if(arr[i]>max)
        {
            
        }
    }
}